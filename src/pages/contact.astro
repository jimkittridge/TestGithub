---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
import ContactForm from '../components/ContactForm.astro';
import { getEntry } from 'astro:content';

const page = await getEntry('pages', 'contact');
const cms = page!.data;
---

<BaseLayout title={cms.seo.title} description={cms.seo.description}>
  <Hero
    headline={cms.hero!.headline}
    subheadline={cms.hero!.subheadline}
  />

  <ContactForm />

  <section class="section contact-info-section">
    <div class="container">
      <div class="contact-grid">
        <div class="contact-card">
          <div class="contact-icon">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M4 6h20a2 2 0 012 2v12a2 2 0 01-2 2H4a2 2 0 01-2-2V8a2 2 0 012-2z"/>
              <path d="M26 8l-12 7L2 8"/>
            </svg>
          </div>
          <h3>Email</h3>
          <a href={`mailto:${cms.email}`}>{cms.email}</a>
        </div>

        <div class="contact-card">
          <div class="contact-icon">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M22 17.5v2.5a2 2 0 01-2.18 2A18 18 0 014 6.18 2 2 0 016 4h2.5a2 2 0 012 1.72c.13.96.36 1.9.7 2.81a2 2 0 01-.45 2.11L9.09 12.3a16 16 0 006.6 6.6l1.66-1.66a2 2 0 012.11-.45c.91.34 1.85.57 2.81.7A2 2 0 0122 17.5z"/>
            </svg>
          </div>
          <h3>Phone</h3>
          <a href={`tel:${cms.phoneHref}`}>{cms.phone}</a>
        </div>

        <div class="contact-card">
          <div class="contact-icon">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none" stroke="currentColor" stroke-width="1.5">
              <circle cx="14" cy="14" r="10"/>
              <path d="M14 8v6l4 2"/>
            </svg>
          </div>
          <h3>Office Hours</h3>
          <p>{cms.officeHours}</p>
          <p class="text-slate" style="font-size: var(--font-size-xs);">{cms.tenantAccess}</p>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .contact-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-6);
    max-width: 800px;
    margin: 0 auto;
  }

  @media (min-width: 768px) {
    .contact-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .contact-card {
    text-align: center;
    padding: var(--space-6);
    background: var(--light-grey);
    border-radius: var(--radius-lg);
  }

  .contact-icon {
    width: 56px;
    height: 56px;
    margin: 0 auto var(--space-4);
    background: rgba(232, 98, 42, 0.08);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--orange);
  }

  .contact-card h3 {
    font-size: var(--font-size-base);
    margin-bottom: var(--space-2);
  }

  .contact-card a {
    font-size: var(--font-size-base);
    color: var(--orange);
    font-weight: 600;
  }

  .contact-card p {
    font-size: var(--font-size-base);
    color: var(--navy);
  }
</style>
